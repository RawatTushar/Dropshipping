# Optional root .env for docker compose (copy to .env in repo root)
# Overrides backend CORS / redirect URLs when deploying to EC2

FRONTEND_URL=http://16.171.37.166
CLIENT_URL=http://16.171.37.166/admin
